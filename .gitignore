# Debug / temp
.wrangler/
.tmp-shots/
aisar-site/
*.log

# Dependencies & builds
node_modules/
.next/
dist/
build/

# Env & secrets
.env
.env.local
.env.*.local
# .env.production is deliberately NOT ignored: it holds VITE_API_URL,
# a public URL that must be baked into every build. Ignoring it once
# shipped a bundle with the backend tree-shaken out entirely.
*.pem
*.key
**/storage-state.json
**/dash-state.json
**/*-state.json
__pycache__/
*.pyc

# OS
.DS_Store

# Claude Code native worktrees live here; never part of the repo
.claude/worktrees/
